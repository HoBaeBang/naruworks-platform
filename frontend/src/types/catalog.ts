export type Project = {
    slug: string;
    name: string;
    description: string;
    status: string;
};

export type ServiceCatalogItem = {
    slug: string;
    name: string;
    description: string;
    status: string;
};
